import * as React from 'react';
import { Text, View, Button, StyleSheet, TouchableOpacity, TouchableHighlight, SafeAreaView, ScrollView, FlatList} from 'react-native';

export default function Payment(){
    return (
        <View style={styles.container}>
            <Text>hi</Text>
        </View>
    )
}

const styles = StyleSheet.create({
    container: {
        backgroundColor: '#f2f2f2',
        flex: 1
    }
})